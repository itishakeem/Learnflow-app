apiVersion: apps/v1
kind: Deployment
metadata:
  name: debug-agent
  namespace: learnflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: debug-agent
  template:
    metadata:
      labels:
        app: debug-agent
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "debug-agent"
        dapr.io/app-port: "8000"
        dapr.io/log-level: "info"
    spec:
      containers:
      - name: debug-agent
        image: debug-agent:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: PUBSUB_NAME
          value: "kafka-pubsub"
        - name: TOPIC_NAME
          value: "code.debug.analysis"
        - name: GEMINI_MODEL
          value: "gemini-1.5-flash"
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: gemini-secret
              key: api-key
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "192Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: debug-agent
  namespace: learnflow
spec:
  selector:
    app: debug-agent
  ports:
  - port: 8000
    targetPort: 8000
